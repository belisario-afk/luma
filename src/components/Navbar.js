// Navbar.js - Reserved for future expansion. The UIManager already renders the navbar.
// This module can inject additional elements or shortcuts if needed.

export function Navbar(el) {
  // Example: append right-side quick badge
  // Intentionally minimal: UIManager owns the navbar rendering.
  return el;
}